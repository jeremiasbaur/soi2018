#include <bits/stdc++.h>
using namespace std;

#define DEB(x) //cerr << x << "\n"
#define int long long
#define FOR(i,m) for(int i = 0; i < m ; i++)


signed main(){
  ios_base::sync_with_stdio(false);
  cin.tie(0);

  int zeilen, spalten, mode;
  cin >> zeilen >> spalten >> mode;
}
